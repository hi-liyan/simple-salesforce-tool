# Salesforce + MySQL 多数据源改造计划（非重构版）

## 0. 关键决策（已确认）
- 不采用 JDBC 路线，不做驱动在线下载与 JVM 桥接。
- 关系型数据库统一采用 Rust `sqlx` 实现（首期 `MySQL`，后续可扩展 `PostgreSQL` 等）。
- 前端新增数据源流程调整为 DataGrip 风格：先选择数据源类型，再弹出对应类型的配置窗口。

## 1. 目标与范围
- 在现有架构上新增后端“数据库适配层”，让系统同时支持 `Salesforce` 与 `MySQL`。
- 保持前端主体交互（对象列表、Tab、DataGrid、日志）可复用，只做必要增强。
- 统一对外能力：对象/表浏览、字段描述、查询、增删改查、批量保存、事务提交。
- 为未来扩展 PostgreSQL / SQL Server / SQLite 预留标准接口，不做一次性大重构。

## 2. 现状结论（基于当前代码）
- 后端命令集中在 `src-tauri/src/commands.rs`，通过 `src-tauri/src/main.rs` 的 `generate_handler!` 暴露。
- `AppState` 目前只有 `sf_client`（`src-tauri/src/app_state.rs`），能力天然偏 Salesforce。
- 数据源存储表为 `salesforce_sources`（`src-tauri/src/db.rs`），字段只适配 Salesforce 凭据。
- 前端 API 与类型命名均为 Salesforce 语义（`src/api/index.ts`、`src/types/index.ts`），但核心数据网格是通用 JSON 记录模型，可复用。

## 3. 总体架构设计
## 3.1 新增 Provider 抽象层（后端）
- 新建模块目录：`src-tauri/src/providers/`
- 定义统一 trait（示意）：
  - `list_objects(source) -> Vec<DataObject>`
  - `describe_object(source, object_name) -> ObjectDescribe`
  - `query_records(source, query_text) -> QueryResult`
  - `create_record(source, object_name, values) -> String`
  - `update_record(source, object_name, record_id, values) -> ()`
  - `delete_record(source, object_name, record_id) -> ()`
  - `save_records(source, object_name, creates, updates, deletes?) -> ()`（必须支持事务语义）
- 实现：
  - `salesforce_provider.rs`：复用当前 `salesforce.rs` 逻辑并适配 trait。
  - `mysql_provider.rs`：基于 MySQL 客户端实现同一组接口。
  - `provider_factory.rs`：根据 `source_type` 返回具体 provider。

## 3.2 统一 Source 模型（后端/前端）
- 将“Salesforce 专用 Source”演进为“通用 DataSource”：
  - `id`, `name`, `source_type`, `config_json`, `created_at`, `updated_at`
  - 兼容过渡：保留旧字段读取能力，分阶段去耦。
- `source_type` 初始支持：
  - `salesforce`
  - `mysql`
- `config_json` 示例：
  - Salesforce：`instanceUrl/accessToken/apiVersion/...`
  - MySQL：`host/port/database/username/password/sslMode/...`

## 4. 数据库迁移方案（SQLite）
## 4.1 Schema 迁移
- 新建表：`data_sources`（通用）。
- 保留旧表：`salesforce_sources`（兼容读取）。
- 启动迁移策略：
  - 若 `data_sources` 为空且 `salesforce_sources` 有数据，则自动迁移历史数据到 `data_sources`，`source_type=salesforce`。
- 改造 `db.rs`：
  - `list_sources/get_source/create_source/update_source/delete_source` 全部切到 `data_sources`。

## 4.2 缓存与配置表兼容
- `object_metadata_cache`、`column_visibility_settings` 等仍以 `source_id` 关联，不需要重建结构。
- 仅校验外键依赖是否与新表一致，必要时改为应用层约束。

## 5. 后端改造步骤
## 阶段 A：模型与存储
- 调整 `src-tauri/src/models.rs`：新增 `DataSourceType`、`DataSource`、`DataSourceConfig`。
- 调整 `src-tauri/src/db.rs`：通用 source CRUD + 迁移脚本。
- 调整 `src-tauri/src/app_state.rs`：新增 provider registry / mysql client 管理。

## 阶段 B：命令分发
- 改造 `src-tauri/src/commands.rs`：
  - 保持原命令名不变（降低前端影响）。
  - 在每个命令中：
    1. 根据 `source_id` 读取 source
    2. 由 `provider_factory` 选择 provider
    3. 调用统一接口
- Salesforce CLI 专属命令保留仅对 `salesforce` 可用（`sync_cli_sources/login_cli_org/...`）。

## 阶段 C：MySQL Provider
- 新增依赖：`sqlx`（异步、参数化 SQL、事务能力）。
- 实现能力：
  - 对象列表：读取 `information_schema.tables`
  - 字段描述：读取 `information_schema.columns`
  - 查询：执行 SQL 文本并映射到 `QueryResult`
  - CRUD：参数化 SQL，避免拼接注入
  - 批量保存：显式事务 `BEGIN -> COMMIT/ROLLBACK`
- 主键策略：
  - 首版只支持“单主键表”的行级更新/删除
  - 非单主键表先提示限制，避免错误写入

## 阶段 D：错误与日志统一
- `SystemLog` 增加 provider 维度（`SALESFORCE_API` / `MYSQL_DB`）。
- 统一错误码与前端可读信息：
  - 连接失败、认证失败、SQL 语法错误、事务回滚原因。

## 6. 前端改造步骤
## 6.1 类型与 API
- `src/types/index.ts`：
  - `SalesforceSource` 升级为 `DataSource`（或别名过渡）。
  - 增加 `sourceType`、`config` 类型。
- `src/api/index.ts`：
  - 保持命令名尽量不变，仅扩展 payload 字段。

## 6.2 数据源管理 UI
- `src/components/SourcePanel.tsx`：
  - 新增“数据源类型选择弹窗”（Salesforce / MySQL）。
  - 选中类型后再打开对应配置弹窗（每种类型独立表单与校验规则）。
  - 按类型动态渲染字段与默认值。
  - MySQL 增加连接测试按钮（新后端命令 `test_source_connection`）。

## 6.3 查询与编辑体验
- `src/features/main/SoqlExecutorWorkspace.tsx`、`src/pages/MainPage.tsx`：
  - 查询编辑器按 sourceType 显示 SQL/SOQL 提示文案。
  - 现有 DataGrid 保持复用。
  - Salesforce 专属入口（如 open record page）在 MySQL 下隐藏或禁用。

## 7. 兼容策略（避免大范围回归）
- 对外 Tauri 命令尽量不改名，优先内部实现切换。
- 前端旧状态持久化字段保留读取兼容，新增字段提供默认值。
- 分阶段合入，每阶段可单独回滚。

## 8. 测试计划
- 单元测试（Rust）：
  - source 迁移逻辑、provider factory、SQL 构造与参数绑定、事务回滚。
- 集成测试：
  - Salesforce 与 MySQL 各跑一套 CRUD + 批量保存流程。
- 前端回归：
  - 数据源切换、对象加载、Tab 编辑、提交、日志展示。
- 手工验收：
  - 断网/错密/超时/死锁等异常场景。

## 9. 分期交付建议
- M1（基础可跑）：通用 Source + Provider 抽象 + Salesforce 迁移适配（行为不变）。
- M2（MySQL 可用）：表/字段浏览 + SQL 查询 + 单条 CRUD。
- M3（生产可用）：批量保存事务、连接测试、错误分级、日志完善。
- M4（扩展准备）：抽象稳定化与 PostgreSQL 接入模板。

## 10. 风险与控制
- 风险：SQL 注入、主键识别不准、跨数据源行为差异。
- 控制：
  - 强制参数化 SQL
  - 更新/删除前校验主键元数据
  - 明确“能力矩阵”并在 UI 层按 sourceType 限制不可用动作。

## 11. 本轮输出边界
- 本文档仅为改造计划，不包含代码实现与数据库迁移脚本落地。
- 代码实施建议按 `M1 -> M2 -> M3` 顺序推进，降低一次性改动风险。
